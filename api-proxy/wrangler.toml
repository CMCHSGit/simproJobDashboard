name = "simpro-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Secrets are set via `wrangler secret put`, NOT stored here.
# Required secrets:
#   SIMPRO_BASE_URL  — e.g. https://yourcompany.simprosuite.com
#   SIMPRO_API_KEY   — your simPRO API bearer token
